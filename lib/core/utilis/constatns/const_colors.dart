import 'package:flutter/material.dart';

// Primary and secondary colors from the Lafyuu design
const Color primaryBlue = Color(0xFF40BFFF);
const Color secondaryOrange = Color(0xFFFB7181);

// Light mode colors
const Color white = Color(0xFFFFFFFF);
const Color darkBlueGray = Color(0xFF223263);  // For headlines and titles
const Color mutedGray = Color(0xFF9098B1);     // For subtitles and muted text
const Color lightBorder = Color(0xFFEBF0FF);   // For input borders

// Dark mode colors
const Color darkBackground = Color(0xFF121212);  // Main dark background
const Color darkCard = Color(0xFF1E1E1E);        // Card backgrounds in dark mode
const Color lightGray = Color(0xFFB0B0B0);       // For subtitles and muted text in dark mode
const Color darkBorder = Color(0xFF333333);      // For input borders in dark mode